<%= errorMessage %>
<% if (username) { %>
  <p>Hi <%= username%></p>
  <a href="/logout">登出</a>
  <a href="/create">新增</a>
  <a href="/manage">管理</a>
<% } else { %>
  <a href="/login">登入</a>
<% } %>
<a href ="/all">全部文章</a>

<% articles.forEach(article => { %>
  <div><%= article.title%></div>
  <div><%= article.content %></div>
<% }) %>